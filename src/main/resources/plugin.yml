name: ExamplePonderPlugin
version: '${project.version}'
main: dansplugins.playerlore.ExamplePonderPlugin
api-version: 1.13

commands:
  exampleponderplugin:
  epp:

permissions:
  epp.help:
    default: true
